# Code Review — PR #15: One-Line Install Scripts

> Auto-generated by code-review skill | 2026-05-26 | Models: Claude Opus 4.7, GPT-5.5 (Copilot CLI)
> Scope: `scripts/install.sh`, `scripts/install.ps1`, `README.md`, `docs/README.md`

---

## 🤖 Model Performance Summary

| Model | Source | Sections | Findings | Status |
|-------|--------|----------|----------|--------|
| Claude Opus 4.7 | Anthropic subagent | 9/9 | 30 | ✅ Valid |
| GPT-5.5 | Copilot CLI | 9/9 | 23 | ✅ Valid |

---

## Summary Scorecard

| Dimension | Opus | GPT-5.5 | Merged | Justification |
|-----------|------|---------|--------|---------------|
| Correctness | 6/10 | 6/10 | **6/10** | `systemctl` check always exits 0 (breaks `--yes` on Linux), `(( i++ ))` `set -e` trap, `$Profile` PS shadowing, sed metachar injection |
| Security | 5/10 | 6/10 | **5/10** | sed/regex key corruption, no `chmod 600`, no supply-chain warning, no `set +x` guard |
| Idempotency | 8/10 | 6/10 | **7/10** | Re-runs reprompt for key even when already set; `git pull --ff-only` fails detached HEAD; otherwise solid |
| Cross-platform consistency | 6/10 | 6/10 | **6/10** | `--dir` vs `-InstallDir` naming mismatch, `INSTALL_DIR` env override bash-only, PS CRLF/encoding gaps |
| Documentation accuracy | 7/10 | 5/10 | **6/10** | `--yes`, `--llm-port`, `--embedding-port` undocumented; no CI/CD example; qwen-local docs say "pull models first" (script does it); no `curl\|bash` warning |
| **Overall** | **6/10** | **6/10** | **6/10** | Functional happy path; needs the fixes below before reliable public one-line use |

---

## Critical / High Findings

| ID | Sev | Confirmed | File | Line | Description | Fix |
|----|-----|-----------|------|------|-------------|-----|
| C1 | **Critical** | Both | `install.sh` | 101 | `systemctl list-unit-files ollama.service` always exits 0 even when the unit doesn't exist — on `--yes` runs this causes `sudo systemctl start ollama` to fail with "Unit not found" and a hung sudo prompt | Change to `systemctl list-unit-files ollama.service \| grep -q ollama.service` |
| C2 | **Critical** | Both | `install.ps1` | 5 | `$Profile` shadows PowerShell's automatic `$PROFILE` variable — confusing, PSScriptAnalyzer warning, breaks dot-source scenarios | Rename to `$AiProfile` |
| C3 | **High** | Both | `install.sh` | 193 | `sed "s\|...\|OPENAI_API_KEY=$_oai_key\|"` — `\|` and `&` in the key corrupt or break `.env.local` write. Current `sk-…` keys are alphanumeric so low practical risk, but it's a correctness bug | Escape key: `_oai_key_esc=$(printf '%s' "$_oai_key" \| sed 's/[\\\&\|]/\\&/g')` then substitute `$_oai_key_esc` |
| C4 | **High** | Both | `install.ps1` | 186 | `.NET` `-replace` treats `$` in the replacement string as a capture-group reference — a key containing `$0`, `$1` etc. gets silently corrupted | Use `[regex]::Escape()` on the replacement or literal `$_ ` substitute |
| C5 | **High** | Opus | `install.sh` | 101–108 | `(( i++ )) \|\| true` — when `i=0`, `(( 0 ))` returns exit code 1; under `set -e` the loop exits immediately without the `\|\| true`. The guard saves it today but the idiom is fragile | Replace with `i=$((i+1))` |
| C6 | **High** | Opus | `install.ps1` | 188, 190 | Appended newline uses `` `n `` (LF) but `.env.local` on Windows is CRLF; `Set-Content` default encoding in PS 5.1 is ANSI not UTF-8 | Use `` `r`n `` and add `-Encoding utf8` to `Set-Content` |

---

## Medium Findings

| ID | Sev | Confirmed | File | Line | Description | Fix |
|----|-----|-----------|------|------|-------------|-----|
| M1 | Medium | Both | `install.sh` | 84–89 | `git pull --ff-only` fails silently for detached HEAD, local modifications, or non-git directory reuse — leaves install in unclear state | Wrap in a `git pull … \|\| warn "Could not pull — using existing state"` |
| M2 | Medium | Opus | `install.sh` | 104 | `ollama serve … & disown` — on some systems SIGHUP on parent exit kills the child despite redirects | Use `nohup ollama serve >/dev/null 2>&1 &` |
| M3 | Medium | Both | `install.sh` | 177–198 | Re-running the script reprompts for the OpenAI key even when `.env.local` already contains a valid one | Check `grep -q "^OPENAI_API_KEY=." .env.local` first and skip |
| M4 | Medium | Opus | `install.sh` | 193 | No `chmod 600 .env.local` after writing the API key — file is world-readable by default on Linux | Add `chmod 600 .env.local` after write |
| M5 | Medium | Both | `install.ps1` | 100–104 | `winget install` missing `--disable-interactivity` / `--silent` — can still prompt in headless CI | Add `--silent` to winget calls |
| M6 | Medium | GPT-5.5 | `install.sh` | 42–58 | If Node exists but is too old, nvm is not attempted — hard error instead of recovery | Source and check nvm before the version check too, not only when node is absent |
| M7 | Medium | Both | `README.md` / `docs/README.md` | — | `--yes`, `--llm-port`, `--embedding-port` flags completely undocumented — key CI/CD flag invisible to users | Add to "One-Liner Options" section |
| M8 | Medium | Both | `README.md` / `docs/README.md` | — | No CI/CD GitHub Actions example anywhere in the docs | Add YAML snippet under "One-Liner Options" |
| M9 | Medium | GPT-5.5 | `docs/README.md` | 59–60 | "pull models first" instruction in the qwen-local quick-start is wrong — the script now pulls models automatically | Remove the manual pull steps from the one-liner path |
| M10 | Medium | Both | `README.md` / `docs/README.md` | — | No `curl \| bash` security note — not even a "you can review this script first" sentence | Add one-liner note |

---

## Low Findings

| ID | Sev | Confirmed | File | Line | Description |
|----|-----|-----------|------|------|-------------|
| L1 | Low | Opus | `install.sh` | 99 | `&>/dev/null 2>&1` — `&>` already redirects both streams; trailing `2>&1` is redundant |
| L2 | Low | Opus | `install.sh` | 101 | Same duplicate redirect |
| L3 | Low | Opus | `install.ps1` | 176–178 | BSTR from `SecureStringToBSTR` is not explicitly freed with `ZeroFreeBSTR` — GC handles it but the best-practice is explicit release |
| L4 | Low | GPT-5.5 | `install.sh` / `install.ps1` | 14–23 / 4–10 | `--dir` (bash) vs `-InstallDir` (PS) — inconsistent naming across platforms; neither documented |
| L5 | Low | GPT-5.5 | `install.sh` | 8 | `INSTALL_DIR` env override supported in bash but not in PowerShell |
| L6 | Low | Opus | `install.sh` | 16–19 | `--profile`, `--dir` etc. with no value use `$2` unguarded — `set -u` gives unhelpful "unbound variable" error |
| L7 | Low | Opus | `install.sh` | — | No profile enum validation — unknown profiles fall through to setup and fail later with a confusing npm error |
| L8 | Low | Opus | `install.sh` | — | No `set +x` guard around key write — if a caller enables `set -x` the key appears in stderr |
| L9 | Low | GPT-5.5 | `docs/README.md` | — | PowerShell llama.cpp one-liner example missing from docs (bash has one) |

---

## No Issues Found

- **Node version comparison logic** (`install.sh:64–79`) — correctly accepts v21+, v20.19+, v20.18.1; rejects v20.18.0. Confirmed by both models.
- **`ask()` with `--yes`** — all interactive prompts including the OpenAI key prompt are correctly bypassed.
- **`_oai_key="${OPENAI_API_KEY:-}"`** — safe under `set -u`.
- **`ollama pull` idempotency** — no-op if model already current.
- **llama.cpp env passthrough** — both scripts correctly set `LLM_BASE_URL`/`EMBEDDING_BASE_URL` before setup.
- **PowerShell CRLF regex** — `(?m)^OPENAI_API_KEY=.*` correctly matches line starts on both LF and CRLF files.
- **Command injection** — `--dir`, `--profile`, ports are all safely quoted or type-bound in both scripts.
- **TODOs/FIXMEs** — none found in any of the 4 files.
- **`read -rsp` / `Read-Host -AsSecureString`** — key not echoed, not stored in shell history.

---

## Fixes to Apply (priority order)

1. **C1** — `systemctl` grep fix (`install.sh:101`)
2. **C2** — Rename `$Profile` → `$AiProfile` (`install.ps1`)
3. **C3** — Escape sed replacement for OpenAI key (`install.sh:193`)
4. **C4** — Escape PS regex replacement for OpenAI key (`install.ps1:186`)
5. **C5** — `(( i++ ))` → `i=$((i+1))` (`install.sh:108`)
6. **C6** — CRLF + UTF-8 encoding in PS (`install.ps1:188, 190`)
7. **M2** — `nohup` for ollama daemon (`install.sh:104`)
8. **M3** — Skip key prompt if already in `.env.local` (both scripts)
9. **M4** — `chmod 600 .env.local` (`install.sh`)
10. **M5** — `winget --silent` (`install.ps1`)
11. **M7/M8/M9/M10** — Docs: document flags, add CI/CD snippet, fix qwen-local, add curl\|bash note

---

## Review Metadata

- **Generated**: 2026-05-26
- **Models**: Claude Opus 4.7, GPT-5.5 (Copilot CLI) — both valid
- **Files reviewed**: 4/4
- **Lines of code**: 1,292 (across all 4 files)
- **Copilot Premium requests**: 1
